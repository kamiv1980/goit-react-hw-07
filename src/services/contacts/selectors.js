/** @format */

export const selContacts = (state) => state.contacts;
export const selFilter = (state) => state.notes.filter;
export const selModal = (state) => state.notes.modal;
export const selEditContact = (state) => state.notes.editItem;
export const selIsLoading = (state) => state.loading.state;
